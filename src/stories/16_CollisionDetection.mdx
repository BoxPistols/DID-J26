{/* 16_CollisionDetection.mdx */}
import { Meta } from '@storybook/blocks'
import { TableStyle } from './components/TableStyle'

<Meta title="Learning/16_衝突検出" />

# Collision Detection（衝突検出）

ドローン飛行計画において、Waypoint・飛行経路・飛行範囲が禁止エリアと重複していないかを検出するユーティリティ関数群です。

---

## 1. 概要

`src/lib/utils/collision.ts` は以下の機能を提供します。

<table style={TableStyle.table}>
  <thead style={TableStyle.thead}>
    <tr>
      <th style={TableStyle.th}>関数名</th>
      <th style={TableStyle.th}>用途</th>
      <th style={TableStyle.th}>計算量</th>
    </tr>
  </thead>
  <tbody>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>checkWaypointCollision</code></td>
      <td style={TableStyle.td}>単一Waypointの禁止エリア判定</td>
      <td style={TableStyle.td}>O(n)</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>checkWaypointCollisionOptimized</code></td>
      <td style={TableStyle.td}>空間インデックスを使った高速判定</td>
      <td style={TableStyle.td}>O(log n)</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>checkPathCollision</code></td>
      <td style={TableStyle.td}>飛行経路（LineString）の交差判定</td>
      <td style={TableStyle.td}>O(n)</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>checkPolygonCollision</code></td>
      <td style={TableStyle.td}>飛行範囲（Polygon）の重複判定</td>
      <td style={TableStyle.td}>O(n)</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>createSpatialIndex</code></td>
      <td style={TableStyle.td}>RBush空間インデックスの構築</td>
      <td style={TableStyle.td}>O(n log n)</td>
    </tr>
  </tbody>
</table>

---

## 2. 依存ライブラリ

```bash
npm install @turf/turf rbush
```

- **@turf/turf**: GeoJSON空間演算ライブラリ（Point-in-Polygon、Intersect等）
- **rbush**: 高速R-tree空間インデックス（大量ポリゴンの高速検索）

---

## 3. ゾーンタイプと色定義

衝突検出結果は、ゾーンタイプに応じた色と深刻度を返します。

<table style={TableStyle.table}>
  <thead style={TableStyle.thead}>
    <tr>
      <th style={TableStyle.th}>ゾーンタイプ</th>
      <th style={TableStyle.th}>色</th>
      <th style={TableStyle.th}>カラーコード</th>
      <th style={TableStyle.th}>深刻度</th>
      <th style={TableStyle.th}>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>DID</code></td>
      <td style={TableStyle.td}>赤</td>
      <td style={TableStyle.td}>#f44336</td>
      <td style={TableStyle.td}>WARNING</td>
      <td style={TableStyle.td}>人口集中地区（許可取得で飛行可能）</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>AIRPORT</code></td>
      <td style={TableStyle.td}>紫</td>
      <td style={TableStyle.td}>#9C27B0</td>
      <td style={TableStyle.td}>DANGER</td>
      <td style={TableStyle.td}>空港周辺空域（原則飛行禁止）</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>RED_ZONE</code></td>
      <td style={TableStyle.td}>赤</td>
      <td style={TableStyle.td}>#FF0000</td>
      <td style={TableStyle.td}>DANGER</td>
      <td style={TableStyle.td}>飛行禁止区域（小型無人機等飛行禁止法）</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}><code style={TableStyle.code}>YELLOW_ZONE</code></td>
      <td style={TableStyle.td}>黄色</td>
      <td style={TableStyle.td}>#ffc107</td>
      <td style={TableStyle.td}>WARNING</td>
      <td style={TableStyle.td}>飛行注意区域（事前通報が必要）</td>
    </tr>
  </tbody>
</table>

```typescript
import { ZONE_COLORS, ZONE_SEVERITY, ZONE_PRIORITY, CollisionType } from 'japan-drone-map'

// ゾーンタイプの定義
type CollisionType = 'DID' | 'AIRPORT' | 'RED_ZONE' | 'YELLOW_ZONE' | 'MILITARY' | 'PARK' | string

// 色定義
const ZONE_COLORS = {
  DID: '#f44336',
  AIRPORT: '#9C27B0',
  RED_ZONE: '#FF0000',
  YELLOW_ZONE: '#ffc107',
  DEFAULT: '#f44336'
}

// ゾーン優先順位（数値が小さいほど優先）
const ZONE_PRIORITY = {
  RED_ZONE: 1,    // 最優先（飛行禁止）
  AIRPORT: 2,     // 空港周辺
  DID: 3,         // 人口集中地区
  YELLOW_ZONE: 4, // 注意区域
  DEFAULT: 99
}
```

### 3.1 ゾーン優先順位

複数のゾーンが重なる場合、優先順位に基づいて最も重要なゾーンが返されます。

<table style={TableStyle.table}>
  <thead style={TableStyle.thead}>
    <tr>
      <th style={TableStyle.th}>優先順位</th>
      <th style={TableStyle.th}>ゾーンタイプ</th>
      <th style={TableStyle.th}>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>1（最優先）</td>
      <td style={TableStyle.td}><code style={TableStyle.code}>RED_ZONE</code></td>
      <td style={TableStyle.td}>飛行禁止区域 - 法律で禁止されている区域</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>2</td>
      <td style={TableStyle.td}><code style={TableStyle.code}>AIRPORT</code></td>
      <td style={TableStyle.td}>空港周辺空域 - 航空法で制限</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>3</td>
      <td style={TableStyle.td}><code style={TableStyle.code}>DID</code></td>
      <td style={TableStyle.td}>人口集中地区 - 許可取得で飛行可能</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>4</td>
      <td style={TableStyle.td}><code style={TableStyle.code}>YELLOW_ZONE</code></td>
      <td style={TableStyle.td}>注意区域 - 事前通報が必要</td>
    </tr>
  </tbody>
</table>

**使用例:** RED_ZONEとYELLOW_ZONEが重なるエリアでは、RED_ZONEが返されます。

---

## 4. Waypoint衝突検出（基本版）

単一のWaypoint座標が禁止エリア内にあるかを判定します。

### 4.1 シグネチャ

```typescript
function checkWaypointCollision(
  waypointCoords: [number, number],  // [経度, 緯度]
  prohibitedAreas: FeatureCollection
): WaypointCollisionResult
```

### 4.2 戻り値の構造

```typescript
interface WaypointCollisionResult {
  isColliding: boolean          // 衝突しているか
  collisionType: CollisionType | null  // ゾーンタイプ
  areaName?: string             // エリア名（例: "東京都港区"）
  severity: 'DANGER' | 'WARNING' | 'SAFE'
  uiColor: string               // UI表示用カラー
  message: string               // 日本語メッセージ
}
```

### 4.3 使用例

```typescript
import { checkWaypointCollision } from 'japan-drone-map'

// 禁止エリアのGeoJSONを準備
const prohibitedAreas: FeatureCollection = {
  type: 'FeatureCollection',
  features: [
    {
      type: 'Feature',
      properties: {
        zoneType: 'DID',
        name: '東京都渋谷区'
      },
      geometry: {
        type: 'Polygon',
        coordinates: [[[139.69, 35.65], [139.72, 35.65], [139.72, 35.68], [139.69, 35.68], [139.69, 35.65]]]
      }
    }
  ]
}

// Waypoint座標（渋谷駅付近）
const waypoint: [number, number] = [139.7015, 35.6580]

const result = checkWaypointCollision(waypoint, prohibitedAreas)

if (result.isColliding) {
  console.log(`警告: ${result.message}`)
  console.log(`ゾーン: ${result.collisionType}`)
  console.log(`深刻度: ${result.severity}`)
  // => 警告: このWaypointは東京都渋谷区内にあります
  // => ゾーン: DID
  // => 深刻度: WARNING
}
```

### 4.4 アルゴリズム詳細

```
1. Waypointの座標からTurf.jsのPointオブジェクトを生成
   point = turf.point([lng, lat])

2. FeatureCollection内の全Polygon/MultiPolygonをループ
   for each feature in prohibitedAreas.features:

3. turf.booleanPointInPolygon() で包含判定
   isInside = turf.booleanPointInPolygon(point, feature)

4. 衝突するすべてのゾーンを収集
   if (isInside):
     collisions.push({ zoneType, areaName, priority })

5. 優先順位でソートし、最も優先度の高いゾーンを返却
   collisions.sort((a, b) => a.priority - b.priority)
   return { isColliding: true, collisionType: highest.zoneType, ... }

6. ヒットしない場合はSAFEを返却
   return { isColliding: false, severity: 'SAFE', ... }
```

**優先順位について:** 複数のゾーンが重なる場合（例：RED_ZONEとYELLOW_ZONEが重複）、
`ZONE_PRIORITY`に基づいて最も優先度の高いゾーンが返されます。

---

## 5. Waypoint衝突検出（最適化版）

大量の禁止エリアがある場合、空間インデックス（R-tree）を使って高速に判定します。

### 5.1 空間インデックスの構築

```typescript
import { createSpatialIndex, checkWaypointCollisionOptimized } from 'japan-drone-map'

// 初回のみ: 空間インデックスを構築（O(n log n)）
const spatialIndex = createSpatialIndex(prohibitedAreas)

// 以降の判定は O(log n) で高速
const result = checkWaypointCollisionOptimized(waypoint, spatialIndex)
```

### 5.2 パフォーマンス比較

<table style={TableStyle.table}>
  <thead style={TableStyle.thead}>
    <tr>
      <th style={TableStyle.th}>禁止エリア数</th>
      <th style={TableStyle.th}>基本版</th>
      <th style={TableStyle.th}>最適化版</th>
      <th style={TableStyle.th}>高速化率</th>
    </tr>
  </thead>
  <tbody>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>100件</td>
      <td style={TableStyle.td}>~1ms</td>
      <td style={TableStyle.td}>~0.1ms</td>
      <td style={TableStyle.td}>10倍</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>1,000件</td>
      <td style={TableStyle.td}>~10ms</td>
      <td style={TableStyle.td}>~0.2ms</td>
      <td style={TableStyle.td}>50倍</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>10,000件</td>
      <td style={TableStyle.td}>~100ms</td>
      <td style={TableStyle.td}>~0.3ms</td>
      <td style={TableStyle.td}>333倍</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>全国DID（~50,000件）</td>
      <td style={TableStyle.td}>~500ms</td>
      <td style={TableStyle.td}>~0.5ms</td>
      <td style={TableStyle.td}>1000倍</td>
    </tr>
  </tbody>
</table>

### 5.3 RBushアルゴリズム

RBush（R-tree Bulk loading using Sort-Tile-Recursive）は、空間データの高速検索に特化したデータ構造です。

```
                    ┌─────────────────────────────────────┐
                    │            Root Node               │
                    │  BBox: [minX, minY, maxX, maxY]    │
                    └─────────────────┬───────────────────┘
                                      │
              ┌───────────────────────┼───────────────────────┐
              ▼                       ▼                       ▼
    ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
    │   Leaf Node 1   │     │   Leaf Node 2   │     │   Leaf Node 3   │
    │ [Features...]   │     │ [Features...]   │     │ [Features...]   │
    └─────────────────┘     └─────────────────┘     └─────────────────┘
```

**検索アルゴリズム:**

1. 検索対象の点（Waypoint）をBounding Boxとして表現
2. ルートノードから再帰的に、BBoxが重なるノードのみを探索
3. リーフノードに到達したら、実際のPolygonと点の包含判定を実行

---

## 6. 飛行経路の交差判定

飛行経路（LineString）が禁止エリアと交差するかを検出します。

### 6.1 シグネチャ

```typescript
function checkPathCollision(
  pathCoords: Position[],        // [[lng1, lat1], [lng2, lat2], ...]
  prohibitedAreas: FeatureCollection
): PathCollisionResult
```

### 6.2 戻り値の構造

```typescript
interface PathCollisionResult {
  isColliding: boolean
  intersectionPoints: Position[]  // 交差点の座標リスト
  severity: 'DANGER' | 'WARNING' | 'SAFE'
  message: string
}
```

### 6.3 使用例

```typescript
import { checkPathCollision } from 'japan-drone-map'

// 飛行経路: 2点間の直線
const flightPath: Position[] = [
  [139.70, 35.65],  // 出発点
  [139.75, 35.70]   // 到着点
]

const result = checkPathCollision(flightPath, prohibitedAreas)

if (result.isColliding) {
  console.log(`警告: ${result.message}`)
  console.log(`交差点数: ${result.intersectionPoints.length}`)
  // => 警告: 飛行経路が禁止エリアを2箇所で通過
}
```

### 6.4 交差判定の可視化

```
       禁止エリア (Polygon)
    ┌────────────────────────┐
    │                        │
    │    ×───────────────×   │  ← 交差点 2箇所
    │    ▲               ▲   │
    │    │               │   │
────┼────┼───────────────┼───┼────  飛行経路 (LineString)
    │    │               │   │
    │    出発点          到着点│
    │                        │
    └────────────────────────┘
```

---

## 7. 飛行範囲の重複判定

飛行範囲（Polygon）が禁止エリアと重複するかを検出します。

### 7.1 シグネチャ

```typescript
function checkPolygonCollision(
  polygonCoords: Position[][],   // [[[lng, lat], ...]]  外周リング
  prohibitedAreas: FeatureCollection
): PolygonCollisionResult
```

### 7.2 戻り値の構造

```typescript
interface PolygonCollisionResult {
  isColliding: boolean
  overlapArea: number       // 重複面積（平方メートル）
  overlapRatio: number      // 重複割合（0.0 ~ 1.0）
  severity: 'DANGER' | 'WARNING' | 'SAFE'
  message: string
}
```

### 7.3 深刻度の判定基準

<table style={TableStyle.table}>
  <thead style={TableStyle.thead}>
    <tr>
      <th style={TableStyle.th}>重複割合</th>
      <th style={TableStyle.th}>深刻度</th>
      <th style={TableStyle.th}>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>0%</td>
      <td style={TableStyle.td}>SAFE</td>
      <td style={TableStyle.td}>重複なし - 飛行可能</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>0% ~ 20%</td>
      <td style={TableStyle.td}>WARNING</td>
      <td style={TableStyle.td}>一部重複 - 経路変更推奨</td>
    </tr>
    <tr style={TableStyle.tr}>
      <td style={TableStyle.td}>20%以上</td>
      <td style={TableStyle.td}>DANGER</td>
      <td style={TableStyle.td}>大幅重複 - 飛行計画見直し必要</td>
    </tr>
  </tbody>
</table>

### 7.4 使用例

```typescript
import { checkPolygonCollision } from 'japan-drone-map'

// 飛行範囲: 四角形エリア
const flightArea: Position[][] = [[
  [139.70, 35.65],
  [139.72, 35.65],
  [139.72, 35.67],
  [139.70, 35.67],
  [139.70, 35.65]  // 閉じる（最初と同じ座標）
]]

const result = checkPolygonCollision(flightArea, prohibitedAreas)

if (result.isColliding) {
  console.log(`警告: ${result.message}`)
  console.log(`重複面積: ${result.overlapArea.toFixed(0)} m²`)
  console.log(`重複割合: ${(result.overlapRatio * 100).toFixed(1)}%`)
}
```

---

## 8. Reactコンポーネントでの実装パターン

### 8.1 useRefを使ったキャッシュ戦略

```typescript
import { useRef, useMemo, useCallback } from 'react'
import { createSpatialIndex, checkWaypointCollisionOptimized } from 'japan-drone-map'

function FlightPlanner() {
  // GeoJSONキャッシュ（レンダリング間で保持）
  const didGeoJSONCacheRef = useRef<FeatureCollection | null>(null)
  const restrictionGeoJSONCacheRef = useRef<FeatureCollection | null>(null)

  // 禁止エリアを統合（依存が変わったときのみ再計算）
  const prohibitedAreas = useMemo(() => {
    const features = []

    // DIDデータ
    if (didGeoJSONCacheRef.current) {
      features.push(...didGeoJSONCacheRef.current.features.map(f => ({
        ...f,
        properties: { ...f.properties, zoneType: 'DID' }
      })))
    }

    // 空港周辺空域
    if (restrictionGeoJSONCacheRef.current) {
      features.push(...restrictionGeoJSONCacheRef.current.features.map(f => ({
        ...f,
        properties: { ...f.properties, zoneType: 'AIRPORT' }
      })))
    }

    return { type: 'FeatureCollection', features }
  }, [didGeoJSONCacheRef.current, restrictionGeoJSONCacheRef.current])

  // 空間インデックス（禁止エリアが変わったら再構築）
  const spatialIndex = useMemo(() => {
    if (prohibitedAreas.features.length === 0) return null
    return createSpatialIndex(prohibitedAreas)
  }, [prohibitedAreas])

  // Waypoint追加時のハンドラ
  const handleWaypointAdd = useCallback((coords: [number, number]) => {
    if (spatialIndex) {
      const result = checkWaypointCollisionOptimized(coords, spatialIndex)
      if (result.isColliding) {
        showWarning(result.message)
      }
    }
  }, [spatialIndex])

  return (/* UI */)
}
```

---

## 9. エラーハンドリング

### 9.1 無効な入力への対応

```typescript
// ポリゴンの座標が不十分な場合
const result = checkPolygonCollision([[]], prohibitedAreas)
// => { isColliding: false, message: '座標が不十分です' }

// 3点未満（ポリゴンには最低4点必要）
const result = checkPolygonCollision([[[0, 0], [1, 1]]], prohibitedAreas)
// => { isColliding: false, message: '座標が不十分です' }

// 自己交差などの無効なジオメトリ
const result = checkPolygonCollision(invalidCoords, prohibitedAreas)
// => { isColliding: false, message: '無効なポリゴン形状' }
```

### 9.2 GeoJSONプロパティの取得優先順位

ゾーンタイプは以下の優先順位で取得されます：

1. `feature.properties.zoneType`（推奨）
2. `feature.properties.type`（互換性のため）
3. デフォルト: `'DID'`

---

## 10. テスト

### 10.1 基本テスト

```typescript
import { describe, it, expect } from 'vitest'
import { checkWaypointCollision, checkPathCollision, checkPolygonCollision } from './collision'

describe('Collision Detection', () => {
  const testArea: FeatureCollection = {
    type: 'FeatureCollection',
    features: [{
      type: 'Feature',
      properties: { zoneType: 'DID', name: 'テストエリア' },
      geometry: {
        type: 'Polygon',
        coordinates: [[[0, 0], [1, 0], [1, 1], [0, 1], [0, 0]]]
      }
    }]
  }

  it('エリア内のWaypointを検出する', () => {
    const result = checkWaypointCollision([0.5, 0.5], testArea)
    expect(result.isColliding).toBe(true)
    expect(result.collisionType).toBe('DID')
  })

  it('エリア外のWaypointはSAFE', () => {
    const result = checkWaypointCollision([2, 2], testArea)
    expect(result.isColliding).toBe(false)
    expect(result.severity).toBe('SAFE')
  })

  it('経路の交差を検出する', () => {
    const result = checkPathCollision([[-1, 0.5], [2, 0.5]], testArea)
    expect(result.isColliding).toBe(true)
    expect(result.intersectionPoints.length).toBe(2)
  })
})
```

### 10.2 実座標を使った再現性テスト

```typescript
describe('実座標テスト', () => {
  const tokyoStationArea = createTestAreaAroundTokyoStation()

  it('東京駅付近のWaypointはDID内と判定', () => {
    const tokyoStation: [number, number] = [139.7671, 35.6812]
    const result = checkWaypointCollision(tokyoStation, tokyoStationArea)
    expect(result.isColliding).toBe(true)
    expect(result.areaName).toBe('東京駅周辺DID')
  })
})
```

---

## 11. 参考文献・外部リソース

### ライブラリ公式ドキュメント

- [Turf.js 公式ドキュメント](https://turfjs.org/) - GeoJSON空間演算ライブラリ
- [Turf.js booleanPointInPolygon](https://turfjs.org/docs/api/booleanPointInPolygon) - 点の包含判定
- [Turf.js lineIntersect](https://turfjs.org/docs/api/lineIntersect) - ライン交差判定
- [Turf.js intersect](https://turfjs.org/docs/api/intersect) - ポリゴン交差計算
- [RBush GitHub](https://github.com/mourner/rbush) - 高速R-tree空間インデックス

### GeoJSON仕様

- [RFC 7946 - The GeoJSON Format](https://datatracker.ietf.org/doc/html/rfc7946) - GeoJSON公式仕様
- [GeoJSON.io](https://geojson.io/) - GeoJSONエディタ・ビューア

### 空間インデックス・アルゴリズム

- [R-tree (Wikipedia)](https://en.wikipedia.org/wiki/R-tree) - R-treeデータ構造の解説
- [Point in Polygon Algorithm](https://en.wikipedia.org/wiki/Point_in_polygon) - Ray Casting法の解説

### ドローン飛行規制関連

- [国土交通省 無人航空機の飛行ルール](https://www.mlit.go.jp/koku/koku_tk10_000003.html) - 航空法に基づく飛行ルール
- [DIPS 2.0（ドローン情報基盤システム）](https://www.ossportal.dips.mlit.go.jp/) - 飛行許可申請システム
- [国土地理院 地理院タイル](https://maps.gsi.go.jp/development/ichiran.html) - 空港周辺空域タイル等
