# DID Map - Environment Variables Example
# このファイルを .env にコピーして使用してください

# === 現在は環境変数不要 ===
# 本プロジェクトは以下の無料APIを使用しているため、環境変数は不要です：
# - OpenStreetMap Nominatim API（ジオコーディング）
# - 国土地理院タイル（地図タイル）
# - OpenMapTiles（フォント）

# === 将来的な拡張用 ===
# 以下のサービスを追加する場合は、環境変数が必要になります：

# MapTiler API（商用地図タイル）
# VITE_MAPTILER_API_KEY=your_maptiler_api_key_here

# Mapbox API（商用地図タイル・ジオコーディング）
# VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Google Maps API（ジオコーディング・ルート検索）
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Sentry（エラートラッキング）
# VITE_SENTRY_DSN=your_sentry_dsn_here

# === 注意事項 ===
# Viteプロジェクトでは環境変数名は VITE_ で始める必要があります
# Vercelにデプロイする場合は、Vercelの環境変数設定で同じ変数を設定してください
